@model Vidly.Models.Customer
@{
    ViewBag.Title = "Details";
}

<h2>@Model.Name</h2>

<p>Memebership Type: @Model.MembershipType.Name</p>
@if (Model.BirthDate.HasValue)
{
<p>Birth Date: @string.Format("{0:dd/MM/yyyy}", Model.BirthDate) </p>
}
